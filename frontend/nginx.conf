server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # API requests should NOT accidentally be swallowed by the SPA fallback
    location /api/ {
        return 404;
    }
}
